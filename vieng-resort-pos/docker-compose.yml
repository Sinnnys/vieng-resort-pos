services:
  pos:
    image: ghcr.io/sinnnys/vieng-resort-pos:latest
    container_name: vieng-pos
    restart: unless-stopped
    ports:
      - "4001:4001"
    environment:
      - JWT_SECRET=vieng-pos-secret-k3y-2026
    volumes:
      - pos-data:/app/data

volumes:
  pos-data:
